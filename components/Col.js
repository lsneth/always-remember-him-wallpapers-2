import {View, Image} from 'react-native'

export default function Col(props){
    return(
        // for dynamic rendering
        // <Image source={require(path)} style={props.style}></Image>
        null
    )
}